# TODO: Add Drag-to-Scroll to Schedule Page

- [ ] Add states and ref for drag functionality (isDragging, startX, scrollLeft, scrollRef)
- [ ] Implement handleMouseDown, handleMouseMove, handleMouseUp, handleMouseLeave handlers
- [ ] Update the scrollable div with ref, event handlers, and conditional cursor classes
- [ ] Test drag functionality and ensure it doesn't interfere with schedule item clicks
